name: Download and send configs to Telegram

on:
  schedule:
    - cron: '*/10 * * * *'
  workflow_dispatch:

jobs:
  download-and-send:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      # Восстанавливаем ID старых сообщений (если есть)
      - name: Restore message IDs
        id: cache
        uses: actions/cache@v4
        with:
          path: message_ids.txt
          key: message-ids-cache

      # Скачиваем файлы
      - name: Download files
        run: |
          curl -L -o "WhiteListsRusMobile(MostHelpful).txt" \
            "https://raw.githubusercontent.com/igareck/vpn-configs-for-russia/refs/heads/main/Vless-Reality-White-Lists-Rus-Mobile.txt"
          curl -L -o "WhiteListsRusMobile2.txt" \
            "https://raw.githubusercontent.com/igareck/vpn-configs-for-russia/refs/heads/main/Vless-Reality-White-Lists-Rus-Mobile-2.txt"
          curl -L -o "WhiteCidrRUAll.txt" \
            "https://raw.githubusercontent.com/igareck/vpn-configs-for-russia/refs/heads/main/WHITE-CIDR-RU-all.txt"
          curl -L -o "WhiteCidrRUChecked.txt" \
            "https://raw.githubusercontent.com/igareck/vpn-configs-for-russia/refs/heads/main/WHITE-CIDR-RU-checked.txt"
          curl -L -o "WhiteSNIRUAll.txt" \
            "https://raw.githubusercontent.com/igareck/vpn-configs-for-russia/refs/heads/main/WHITE-SNI-RU-all.txt"

      # Удаляем старые сообщения (только если файл с ID существует)
      - name: Delete old messages
        env:
          TG_BOT_TOKEN: ${{ secrets.BOT_TOKEN }}
          TG_CHAT_ID: ${{ secrets.CHAT_ID }}
        run: |
          if [ -f "message_ids.txt" ]; then
            echo "📤 Удаляем старые сообщения..."
            while IFS= read -r msg_id; do
              if [ -n "$msg_id" ]; then
                curl -s -X POST "https://api.telegram.org/bot${TG_BOT_TOKEN}/deleteMessage" \
                  -F "chat_id=${TG_CHAT_ID}" \
                  -F "message_id=${msg_id}" > /dev/null 2>&1
                echo "Удалено сообщение: $msg_id"
              fi
            done < message_ids.txt
          else
            echo "📭 Сохраненных ID сообщений нет (первый запуск или пустой канал)"
          fi

      # Отправляем ВСЕ файлы (всегда) и сохраняем их ID
      - name: Send all files and save message IDs
        env:
          TG_BOT_TOKEN: ${{ secrets.BOT_TOKEN }}
          TG_CHAT_ID: ${{ secrets.CHAT_ID }}
        run: |
          # Очищаем старый файл с ID
          rm -f message_ids.txt
          
          # Отправляем каждый файл
          for file in WhiteListsRusMobile\(MostHelpful\).txt WhiteListsRusMobile2.txt WhiteCidrRUAll.txt WhiteCidrRUChecked.txt WhiteSNIRUAll.txt; do
            if [ -f "$file" ]; then
              echo "📤 Отправляем: $file"
              RESPONSE=$(curl -s -X POST "https://api.telegram.org/bot${TG_BOT_TOKEN}/sendDocument" \
                -F "chat_id=${TG_CHAT_ID}" \
                -F "document=@$file")
              
              # Извлекаем message_id из ответа
              MSG_ID=$(echo "$RESPONSE" | grep -o '"message_id":[0-9]*' | head -1 | cut -d':' -f2)
              if [ -n "$MSG_ID" ]; then
                echo "$MSG_ID" >> message_ids.txt
                echo "✅ Отправлено, ID: $MSG_ID"
              else
                echo "❌ Ошибка при отправке $file"
                echo "Ответ API: $RESPONSE"
              fi
            fi
          done
          
          echo "📝 Сохранено ID сообщений: $(cat message_ids.txt 2>/dev/null | wc -l) шт."

      # Сохраняем ID сообщений в кэш
      - name: Save message IDs to cache
        uses: actions/cache@v4
        with:
          path: message_ids.txt
          key: message-ids-cache
